"""Traduction automatique des articles (via NLLB / Tondo)."""


def translate(text: str, source_lang: str, target_lang: str = "fr") -> str:
    """Traduit un texte vers le français."""
    raise NotImplementedError("À implémenter")
